# Task Model v2 — Progress

## Current Status
US-004 complete

## Stories
- [x] US-001: Define canonical task types and adapter interface
- [x] US-002: Update local database schema for new lifecycle
- [x] US-003: Markdown section helpers
- [x] US-004: Update Notion board schema for new properties
- [ ] US-005: Update Notion adapter to new interface
- [ ] US-006: Update pipe runtime to use new adapter interface
- [ ] US-007: Expose TaskHandle to pipes
- [ ] US-008: Update sync loop for new lifecycle and comment-based feedback

## Notes
- 2026-03-22: Completed US-001 by replacing the task-board contract with `TaskLifecycle`, `TaskBoardPatch`, and `{id, title, artifact, comments}` snapshots; updated the null adapter, the Notion adapter, and the runtime call sites to use `updateTask` / `writeArtifact`.
- 2026-03-22: Tracer-bullet scope only. The runtime now maps legacy internal board states (`running`, `feedback`, `blocked`) into canonical lifecycle patches so the new adapter contract is live without pulling the later lifecycle/schema stories fully forward.
- 2026-03-22 verification: `npm run check` ✅
- 2026-03-22 verification: `npx vitest run src/core/taskBoardAdapter.test.ts src/adapters/notion.test.ts src/core/pipeRuntime.test.ts` ✅
- 2026-03-22: Completed US-002 by migrating local SQLite lifecycle storage from legacy `running` / `feedback` / `blocked` values to canonical `in_progress` / `needs_input`, adding bootstrap migration updates for existing `tasks`, `runs`, and `run_traces` rows, and updating the runtime, Notion sync loop, and dashboard queries to persist/query the canonical lifecycle values.
- 2026-03-22: Kept the current Notion board schema as a compatibility layer for now. Local state is canonical, while shared-board writes still translate through the older board status labels until the later Notion schema stories land.
- 2026-03-22 verification: `npm run check` ✅
- 2026-03-22 verification: `npx vitest run src/app/context.test.ts src/commands/notion.test.ts src/core/pipeRuntime.test.ts src/core/runTraces.test.ts src/dashboard/snapshot.test.ts src/services/statusIcons.test.ts` ✅
- Pre-release project — no migration or backward compatibility needed
- US-002 moved before US-003 since DB schema is a dependency for runtime changes
- US-003 (section helpers) has no dependencies and can land early as a pure utility
- 2026-03-22: Completed US-003 by adding pure markdown `##` section helpers in `src/core/artifactSections.ts` for reading, replacing/appending, and removing artifact sections with canonical serialization.
- 2026-03-22: Tracer-bullet scope only. This lands the helper API and test coverage without wiring section-level artifact editing into the runtime yet.
- 2026-03-22 verification: `npm run check` ✅
- 2026-03-22 verification: `npx vitest run src/core/artifactSections.test.ts` ✅
- 2026-03-22: Completed US-004 by updating shared Notion board provisioning to the new property set: `Status`, `Pipe`, `Current Action`, `Progress`, and `PR`; lifecycle status options now use `Queue` / `In Progress` / `Needs Input` / `Done` / `Failed`, and task creation now writes `Status` instead of `State`.
- 2026-03-22: Tracer-bullet scope only. Added compatibility shims so existing flows can still read legacy `State` values while new boards and updates move onto `Status` and `Current Action`.
- 2026-03-22 verification: `npm run check` ✅
- 2026-03-22 verification: `npx vitest run src/services/notion.test.ts src/commands/notion.test.ts` ✅
