Workspace-Backed Pipe Execution Progress:
- Branch: ralph/workspace-backed-execution
- Source PRD: tasks/prd-workspace-execution.md
- Status: in progress
- Completed:
  - US-001: Added workspace config parsing in `src/project/projectConfig.ts` for omitted workspace, string shorthand, and object overrides. Normalized workspace config now defaults to project-repo mode, supports explicit repo/ref/cwd/cleanup overrides, and exposes `resolveWorkspaceConfig` to resolve the containing git repo for project mode and absolute local repo paths for explicit mode.
  - US-001: Added config diagnostics for invalid workspace values and path-aware union error flattening so malformed workspace keys surface actionable `ProjectConfigLoadError` messages.
  - US-001: Added unit coverage in `src/project/projectConfig.test.ts` for default project-repo resolution, local and remote shorthand repos, object overrides, and invalid workspace diagnostics.
  - US-002: Extended runtime path support in `src/config/paths.ts` and `src/app/context.ts` so `.notionflow` now provisions dedicated directories for managed mirrors, workspace manifests, and per-run worktrees.
  - US-002: Added `src/core/workspaceRuntime.ts` to validate workspace repo sources, create or refresh managed bare mirrors, materialize run-scoped git worktrees, and persist workspace manifests with root/cwd/ref metadata.
  - US-002: Added unit coverage in `src/core/workspaceRuntime.test.ts` for project-repo mode, explicit repo mode, invalid repo sources, and unresolved refs.
  - US-003: Updated `src/core/pipeRuntime.ts` so `runPipeTaskByExternalId` resolves the active project config from the runtime project root, provisions the run workspace keyed by `runId`, and does so immediately before `pipeDefinition.run(...)` executes.
  - US-003: Updated `src/core/pipeRuntime.test.ts` to run against temporary git-backed projects and added coverage that verifies workspace manifests/worktrees exist before pipe execution and that project-repo workspaces map monorepo `projectRoot` paths to the correct workspace `cwd`.
  - US-004: Updated `src/core/pipeRuntime.ts` so resumed feedback/checkpoint ticks reuse the most recent open run record, preserving the original `runId` and workspace path instead of minting a fresh run on each resume.
  - US-004: Updated `src/core/workspaceRuntime.ts` so resumed runs fail if their original workspace directory is missing, with an actionable error that includes the run id and workspace path, and canonicalized managed-worktree path checks so existing macOS worktrees are recognized on resume.
  - US-004: Added coverage in `src/core/pipeRuntime.test.ts` that verifies resumed runs keep the same workspace across feedback loops and that deleting the original resume workspace fails the run instead of silently recreating it.
  - US-005: Extended `src/pipe/canonical.ts` with public `PipeWorkspace` and `PipeWorkspaceSource` types, made `PipeInput.workspace` a typed runtime handle, and re-exported the new types from `src/index.ts`.
  - US-005: Updated `src/core/pipeRuntime.ts` to map each provisioned run workspace into `PipeInput.workspace` before pipe execution, including default project-repo workspace provisioning when runtime execution has no resolved config file.
  - US-005: Added runtime coverage in `src/core/pipeRuntime.test.ts` proving pipes receive the workspace handle with root/cwd/ref/source metadata and that runtime execution does not mutate `process.cwd()`.
  - US-005: Updated canonical, smoke, and e2e contract fixtures to provide the required workspace handle so the new `PipeInput` API typechecks across test coverage.
- Notes:
  - Verification:
    - `npm run check` (pass)
    - `npx vitest run src/project/projectConfig.test.ts` (pass)
    - `npx vitest run src/core/workspaceRuntime.test.ts` (pass)
    - `npx vitest run src/core/pipeRuntime.test.ts` (pass)
    - `npm run check` (pass)
    - `npx vitest run src/core/workspaceRuntime.test.ts` (pass)
    - `npx vitest run src/core/pipeRuntime.test.ts` (pass)
    - `npm run check` (pass)
    - `npx vitest run src/core/pipeRuntime.test.ts src/pipe/canonical.test.ts src/pipe/example-factories.smoke.test.ts` (pass)
